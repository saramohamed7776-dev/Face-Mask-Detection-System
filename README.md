# Face-Mask-Detection-System
Built a real-time face mask detection system using deep learning and OpenCV. The model uses a webcam to detect faces in live video and classifies each face as “Mask” or “No Mask” with a confidence score, displaying results instantly with colored
